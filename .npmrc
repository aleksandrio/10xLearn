# Fail `npm install` loudly on an unsupported Node version instead of letting it
# surface later as an opaque Astro build error. Pairs with "engines" in
# package.json and the version pinned in .nvmrc.
engine-strict=true
