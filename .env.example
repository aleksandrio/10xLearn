SUPABASE_URL=###
SUPABASE_KEY=###
# HMAC secret signing the guest-progress cookie. Optional in dev (falls back to
# an insecure default); set a strong random value in the deploy environment.
GUEST_PROGRESS_SECRET=###